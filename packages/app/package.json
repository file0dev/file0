{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "ssc build -r -o --test=./test/index.js --headless",
    "init-project": "ssc init --config",
    "start": "ssc build -r -o",
    "build": "rimraf ./src && mv ../client/dist  ./src && ssc build -o"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.20.2",
    "rimraf": "^5.0.5"
  },
  "type": "module"
}
